# Logfire (Pydantic Logfire)

You have Logfire tools prefixed `logfire_*` for querying the user's own
application traces, spans, metrics, and logs. Use only when the user asks
about their application's observability data.

Summarize results — don't dump raw rows unless the user asks for them.
